# Diabetes-prediction-project
I built a machine learning model that predicts whether a person has diabetes or not. I used health data like age, BMI, and blood sugar levels to train models like Random Forest and Logistic Regression. The model learns patterns from data and gives a yes or no answer.
